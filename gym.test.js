const { saveClasses, createTables } = require("./gym.js");
const _ = require("lodash");

test("It should create tables in the database", () => {});

test("It should save classes to the database", () => {
  expect(1).toBe(1);
});
